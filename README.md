# Byte-Sized RSE: Python Packaging

This is a Byte-Sized RSE session on how to package Python scripts, libraries and applications, using tools like `pip`, `build`, `twine` and `briefcase`.

It also discusses how to specify project dependencies and how to add a `pyproject.toml` file to your project.
